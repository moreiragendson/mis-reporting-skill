# Normalise line endings so the skill folder and its mirrors stay byte-identical
# across platforms.
* text=auto eol=lf

# Windows-only script keeps CRLF
*.ps1 text eol=crlf

*.png binary
*.jpg binary
*.pbix binary
*.twbx binary
*.hyper binary
